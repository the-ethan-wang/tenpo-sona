# Program ideas

- simple wiki-scraper for saving wiki pages, probably just use selenium's `driver.page_source` and save as html file with the wiki endpoint as the `filename.html`
- library of babel type project but for something else
- tetris opener analyser and finder and pc finder lol
- classical music library but good
- minecraft seedfinding. I've never even looked into this at all so im looking forward to it